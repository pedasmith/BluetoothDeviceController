<UserControl
    x:Class="BluetoothDeviceController.PerDeviceSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BluetoothDeviceController"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel>
        <TextBox Header="Name" x:Name="uiName" KeyDown="UiName_KeyDown" ></TextBox>
        <TextBlock IsTextSelectionEnabled="True">
            <Run FontWeight="Bold">Device Name:</Run>
            <Run x:Name="uiDeviceName">(name)</Run>
            <LineBreak />
            <Run FontWeight="Bold">MAC address:</Run>
            <Run x:Name="uiDeviceMacAddress">(name)</Run>
            <LineBreak /><Run FontWeight="Bold">Device Id:</Run>
            <Run x:Name="uiDeviceId">(id)</Run>
        </TextBlock>
    </StackPanel>
</UserControl>
