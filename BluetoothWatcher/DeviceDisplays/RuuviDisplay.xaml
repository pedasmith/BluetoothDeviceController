<UserControl
    x:Class="BluetoothWatcher.DeviceDisplays.RuuviDisplay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BluetoothWatcher.DeviceDisplays"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Border BorderBrush="Black" BorderThickness="2" Padding="8" Margin="3" Background="LightBlue" Width="200" Height="200">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <TextBlock><Run>RUUVI</Run> Name=<Run x:Name="uiName" /> N=<Run x:Name="uiCount" /></TextBlock>
            <TextBlock Grid.Row="1">
            <Run>Temperature</Run><Run x:Name="uiTemperature" />
            <LineBreak/><Run>Pressure</Run><Run x:Name="uiPressure" />
            <LineBreak/>
            </TextBlock>
        </Grid>
    </Border>
</UserControl>
