<UserControl
    x:Class="WinUI3Controls.SimpleMapControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI3Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <TextBlock>It's the map, it's the Map, it's the MAP</TextBlock>
        <ScrollView Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                      HorizontalScrollMode="Enabled" HorizontalScrollBarVisibility="Visible"
                      VerticalScrollMode="Enabled" VerticalScrollBarVisibility="Visible"

                      ZoomMode="Enabled" MaxZoomFactor="50" MinZoomFactor=".1" 
                      ViewChanged="OnViewChanged"
                    x:Name="uiZoom"
                      >
            <Grid HorizontalAlignment="Left" VerticalAlignment="Top">
                <Canvas x:Name="uiMapMarkerCanvas" Width="1" Height="1" HorizontalAlignment="Left" VerticalAlignment="Top">
                </Canvas>

                <Canvas x:Name="uiMapCanvas" Width="1" Height="1" HorizontalAlignment="Left" VerticalAlignment="Top">
                    <Polyline x:Name="uiMapPositionPolyline"
                          Points=""
                          Stroke="Blue" StrokeThickness=".1"
                          Canvas.Left="0" Canvas.Top="0"
                          >

                    </Polyline>
                    <Polyline x:Name="uiCursorMostRecent"
                          Points=""
                          Stroke="LightBlue" StrokeThickness="2.5"
                          Canvas.Left="0" Canvas.Top="0" Opacity=".8"
                         >
                    </Polyline>
                    <Polyline x:Name="uiCursorStart"
                          Points=""
                          Stroke="DarkGreen" StrokeThickness="2.5"
                          Canvas.Left="0" Canvas.Top="0" Opacity=".8"
                         >
                    </Polyline>
                </Canvas>

            </Grid>
        </ScrollView>
        <StackPanel Orientation="Horizontal" Grid.Row="2">
            <Button Content="Clear" Click="OnClear" />
            <Button Content="Redraw" Click="OnRedraw" />
            <Button Content="Add Fake" Click="OnAddFakePoints" />
            <TextBlock x:Name="uiLog" MinWidth="200" />
        </StackPanel>
    </Grid>
</UserControl>
