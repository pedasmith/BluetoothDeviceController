<UserControl
    x:Class="WinUI3Controls.SimpleMapControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI3Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"  IsHitTestVisible="True" IsTabStop="True" IsTapEnabled="True" KeyUp="OnKeyUp">

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <TextBlock>It's the map, it's the Map, it's the MAP</TextBlock>
        <Border x:Name="uiZoom" Grid.Row="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                BorderBrush="Magenta" BorderThickness="1" Background="LightGray"
                
                ManipulationMode="Scale,TranslateX,TranslateY"
                ManipulationStarting="OnManipulationStarting"
                ManipulationStarted="OnManipulationStart"
                ManipulationCompleted="OnManipulationComplete"  
                PointerReleased="OnPointerReleased"
                      >

            <Canvas x:Name="uiMapCanvas"  HorizontalAlignment="Stretch" VerticalAlignment="Stretch" SizeChanged="OnMapCanvasSizeChanged" >
                <Canvas x:Name="uiMapItemCanvas" HorizontalAlignment="Left" VerticalAlignment="Top" Width="1" Height="1">
                    <Canvas x:Name="uiMapPositionCanvas" Width="1" Height="1" HorizontalAlignment="Left" VerticalAlignment="Top" />
                    <Polyline x:Name="uiCursorMostRecent"
                              Points=""
                              Stroke="DarkCyan" StrokeThickness="2.5"
                              Canvas.Left="0" Canvas.Top="0" Opacity=".8"
                             />
                    <Polyline x:Name="uiCursorStart"
                              Points=""
                              Stroke="DarkGreen" StrokeThickness="2.5"
                              Canvas.Left="0" Canvas.Top="0" Opacity=".8"
                             />
                    <Ellipse x:Name="uiCursorHighlight" Visibility="Collapsed"
                                     Width="20" Height="20"
                                     Stroke="DodgerBlue"
                                     />
                    <Canvas x:Name="uiMapMarkerCanvas" Width="100" Height="100" HorizontalAlignment="Left" VerticalAlignment="Top" />
                </Canvas>
                <Border x:Name="uiPointInfoBorder" BorderBrush="Gainsboro" Background="LightGoldenrodYellow"
                            Width="400" Visibility="Collapsed" Padding="5">
                    <StackPanel>
                        <TextBlock FontWeight="Bold" FontSize="16">Point Information</TextBlock>
                        <TextBlock x:Name="uiPointInfo" SizeChanged="OnMapCanvasSizeChanged" />
                    </StackPanel>
                </Border>
            </Canvas>

        </Border>
        <StackPanel Grid.Row="2">
            <StackPanel Orientation="Horizontal" >
                <Button Content="Clear" Click="OnClear" />
                <Button Content="Redraw" Click="OnRedraw" />
                <Button Content="CancelFocus" Click="OnCancelFocus" />
                <Button Content="Add Sample" Click="OnAddSamplePoints" />
                <TextBlock x:Name="uiFocus" MinWidth="200" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="uiLog" MinWidth="200" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
