<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="BluetoothWinUI3.TextBlockWithHistory"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BluetoothWinUI3"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="sPanelDisplay" TargetType="Panel">
            <Setter Property="VerticalAlignment" Value="Stretch" />
        </Style>
    </UserControl.Resources>

    <Grid x:Name="uiHistoryBlock">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" x:Name="uiHistoryBlockScrollViewer">
            <toolkit:MarkdownTextBlock x:Name="uiHistoryMarkdown" Text="# Advertisement" 
                                                   OnLinkClicked="OnLinkClicked" 
                                       IsTextSelectionEnabled="True"
                                       
                                                   />

            <!--
            <TextBlock x:Name="uiAdvertisementDetailsTextBlock" Style="{StaticResource sAdvertisementData}" TextAlignment="Left" >Advertisement Details</TextBlock>
            -->
        </ScrollViewer>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Button Content="Prev" Visibility="Collapsed" x:Name="uiDetailPrev" Click="OnPrevHistory"/>
            <Button Content="Next" Visibility="Collapsed" x:Name="uiDetailNext" Click="OnNextHistory"/>
            <Button Content="Catch up" Visibility="Collapsed" x:Name="uiDetailCatchUp" Click="OnCatchupHistory"/>
            <Button Content="Copy" Visibility="Visible" Click="OnCopy"/>
            <TextBlock x:Name="uiLog" />
        </StackPanel>
    </Grid>
</UserControl>
