<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="BluetoothWinUI3.BTTAOPE_CyclingSpeedCadenceControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BluetoothWinUI3"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:oxyplot="using:OxyPlot"
    xmlns:tv="using:WinUI.TableView"
    mc:Ignorable="d"
    >

    <Border Style="{StaticResource sDeviceBorder}" x:Name="rootPanel">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <TextBlock x:Name="uiDeviceName" Grid.Row="0" Text="Cycle Cadence" Style="{StaticResource sDeviceTitle}"/>
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <GridView Grid.Row="0" Style="{StaticResource sDeviceDataList}" x:Name="uiDeviceDataList">
                    <StackPanel Style="{StaticResource sDeviceDataPanel}" Tag="Wheel">
                        <TextBlock Style="{StaticResource sDevicePrimaryTitle}">
                        <Run>Revolution</Run><Run Text="&#160;"/><Run x:Name="uiRevolutionWheelChange" Text="┄"/>
                        </TextBlock>
                        <TextBlock Style="{StaticResource sDevicePrimaryData}"><Run x:Name="uiRevolutionWheel"/></TextBlock>
                    </StackPanel>

                    <StackPanel Style="{StaticResource sDeviceDataPanel}" Tag="Wheel">
                        <TextBlock Style="{StaticResource sDevicePrimaryTitle}">Time</TextBlock>
                        <TextBlock Style="{StaticResource sDevicePrimaryData}"><Run x:Name="uiTimeWheel"/></TextBlock>
                    </StackPanel>

                    <StackPanel Style="{StaticResource sDeviceDataPanel}" Tag="Wheel">
                        <TextBlock Style="{StaticResource sDevicePrimaryTitle}">RPS</TextBlock>
                        <TextBlock Style="{StaticResource sDevicePrimaryData}"><Run x:Name="uiRpsWheel"/></TextBlock>
                    </StackPanel>



                    <StackPanel Style="{StaticResource sDeviceDataPanel}" Tag="Crank">
                        <TextBlock Style="{StaticResource sDevicePrimaryTitle}">
                        <Run>Revolution</Run><Run Text="&#160;"/><Run x:Name="uiRevolutionCrankChange" Text="┄"/>
                        </TextBlock>
                        <TextBlock Style="{StaticResource sDevicePrimaryData}"><Run x:Name="uiRevolutionCrank"/></TextBlock>
                    </StackPanel>

                    <StackPanel Style="{StaticResource sDeviceDataPanel}" Tag="Crank">
                        <TextBlock Style="{StaticResource sDevicePrimaryTitle}">Time</TextBlock>
                        <TextBlock Style="{StaticResource sDevicePrimaryData}"><Run x:Name="uiTimeCrank"/></TextBlock>
                    </StackPanel>

                    <StackPanel Style="{StaticResource sDeviceDataPanel}" Tag="Crank">
                        <TextBlock Style="{StaticResource sDevicePrimaryTitle}">RPS</TextBlock>
                        <TextBlock Style="{StaticResource sDevicePrimaryData}"><Run x:Name="uiRpsCrank"/></TextBlock>
                    </StackPanel>



                    <!-- This StackPanel has a name because it might be removed from the display (not ever
                         device has a Battery service (0000180f-0000-1000-8000-00805f9b34fb) and the
                         \corresponding BatteryLevel characteristic (00002a19-0000-1000-8000-00805f9b34fb)
                    -->
                    <StackPanel Style="{StaticResource sDeviceDataPanel}" >
                        <TextBlock Style="{StaticResource sDevicePrimaryTitle}">
                        <Run>Flags</Run><Run Text=" "/><Run x:Name="uiFlagsChange" Text=" "/>
                        </TextBlock>
                        <TextBlock Style="{StaticResource sDevicePrimaryData}"><Run x:Name="uiFlags"/></TextBlock>
                    </StackPanel>

                </GridView>
                <oxyplot:PlotView Grid.Row="2" x:Name="uiOxyPlot" Background="Transparent" Visibility="Visible"  />
                <Grid Grid.Row="2" x:Name="uiDataGridPanel" Visibility="Collapsed" >
                    <tv:TableView x:Name="uiTableView" IsReadOnly="True" />
                </Grid>

            </Grid>
            <TextBlock Grid.Row="2" Style="{StaticResource sDeviceMinorText}"><Run>Bluetooth Address: </Run><Run x:Name="uiAddress"/></TextBlock>
            <local:BTConnectionControl Grid.Row="3" x:Name="uiBTConnectionControl" />
        </Grid>
    </Border>
</UserControl>

