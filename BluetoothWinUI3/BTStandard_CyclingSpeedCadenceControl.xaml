<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="BluetoothWinUI3.BTStandard_CyclingSpeedCadenceControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BluetoothWinUI3"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:oxyplot="using:OxyPlot"
    xmlns:tv="using:WinUI.TableView"
    mc:Ignorable="d"
    >

    <Border Style="{StaticResource sDeviceBorder}" x:Name="rootPanel">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="auto" />
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="uiKnownDeviceName" Grid.Column="0" Text="Cycling Speed and Cadence" Style="{StaticResource sDeviceTitle}"/>
                <TextBlock x:Name="uiSensorPosition" Grid.Column="1" Text="--" Style="{StaticResource sDeviceTitle}"/>
            </Grid>
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>
                <GridView Grid.Row="0" Style="{StaticResource sDeviceDataList}" x:Name="uiDeviceDataList">
                    <StackPanel Style="{StaticResource sDeviceDataPanel}">
                        <TextBlock Style="{StaticResource sDevicePrimaryTitle}">
                        <Run>RPS (smooth)</Run><Run Text="&#160;"/><Run x:Name="uiRpsSensorChange" Text="┄"/>
                        </TextBlock>
                        <TextBlock Style="{StaticResource sDevicePrimaryData}"><Run x:Name="uiRpsSensorEwma"/></TextBlock>
                    </StackPanel>

                    <StackPanel Style="{StaticResource sDeviceDataPanel}">
                        <TextBlock Style="{StaticResource sDevicePrimaryTitle}">
                        <Run>RPS</Run><Run Text="&#160;"/>
                        </TextBlock>
                        <TextBlock Style="{StaticResource sDevicePrimaryData}"><Run x:Name="uiRpsSensor"/></TextBlock>
                    </StackPanel>

                    <StackPanel Style="{StaticResource sDeviceDataPanel}">
                        <TextBlock Style="{StaticResource sDevicePrimaryTitle}">Revolutions</TextBlock>
                        <TextBlock Style="{StaticResource sDevicePrimaryData}"><Run x:Name="uiRevolutionSensor"/></TextBlock>
                    </StackPanel>

                    <StackPanel Style="{StaticResource sDeviceDataPanel}">
                        <TextBlock Style="{StaticResource sDevicePrimaryTitle}">Speed</TextBlock>
                        <TextBlock Style="{StaticResource sDevicePrimaryData}"><Run x:Name="uiCurrSpeed"/></TextBlock>
                    </StackPanel>

                    <StackPanel Style="{StaticResource sDeviceDataPanel}">
                        <TextBlock Style="{StaticResource sDevicePrimaryTitle}">Distance</TextBlock>
                        <TextBlock Style="{StaticResource sDevicePrimaryData}"><Run x:Name="uiRideDistance"/></TextBlock>
                    </StackPanel>

                    <StackPanel Style="{StaticResource sDeviceDataPanel}">
                        <TextBlock Style="{StaticResource sDevicePrimaryTitle}">Odometer</TextBlock>
                        <TextBlock Style="{StaticResource sDevicePrimaryData}"><Run x:Name="uiTotalDistance"/></TextBlock>
                    </StackPanel>
                </GridView>
                <oxyplot:PlotView Grid.Row="2" x:Name="uiOxyPlot" Background="Transparent" Visibility="Visible"  />
                <Grid Grid.Row="2" x:Name="uiDataGridPanel" Visibility="Collapsed" >
                    <tv:TableView x:Name="uiTableView" IsReadOnly="True" />
                </Grid>

            </Grid>
            <TextBlock Grid.Row="2" Style="{StaticResource sDeviceMinorText}"><Run>Bluetooth Address: </Run><Run x:Name="uiAddress"/></TextBlock>
            <local:BTConnectionControl Grid.Row="3" x:Name="uiBTConnectionControl" />
        </Grid>
    </Border>
</UserControl>
